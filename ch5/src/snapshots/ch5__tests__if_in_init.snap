---
source: ch5/src/lib.rs
expression: prog

---
extern read_int, print_int, print_str, print_bool, print_newline
section .text

    global _start
_start:
    push rbp
    mov rbp, rsp
    sub rsp, 8
    push rbx
    call read_int
    mov rbx, rax
    cmp rbx, 3
    jge block3
    mov rbx, 77

block2:
    cmp rbx, 10
    sete al
    movzx rbx, al
    cmp rbx, 0
    je block1
    mov rax, 2

conclusion:
    pop rbx
    call print_int
    call print_newline
    mov rsp, rbp
    pop rbp
    mov rax, 60
    mov rdi, 0
    syscall

block1:
    mov rax, 41
    jmp conclusion

block3:
    mov rbx, 10
    jmp block2

