---
source: ch3/src/lib.rs
expression: prog

---
extern read_int, print_int, print_newline
section .text

start:
    call read_int
    mov rbx, rax
    mov rbx, 5
    call read_int
    mov rcx, rax
    mov rdx, rcx
    neg rdx
    mov rcx, -2
    add rcx, rbx
    mov rax, rdx
    add rax, rcx
    jmp conclusion

    global _start
_start:
    push rbp
    mov rbp, rsp
    sub rsp, 16
    push rbx
    jmp start

conclusion:
    mov rsp, rbp
    pop rbp
    call print_int
    call print_newline
    mov rax, 60
    mov rdi, 0
    syscall

